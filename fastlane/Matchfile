# Match configuration for iOS code signing
# https://docs.fastlane.tools/actions/match/

git_url(ENV["MATCH_GIT_URL"])

storage_mode("git")

type("development") # Default type, can be overridden

# App identifiers for all brands
app_identifier([
  "com.whitelabel.app",
  "com.branda.app"
])

# Team ID
team_id(ENV["TEAM_ID"])

# For more information about Match, see:
# https://docs.fastlane.tools/actions/match/
